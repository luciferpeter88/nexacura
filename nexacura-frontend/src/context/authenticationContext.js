import { createContext } from "react";
// Creates a new Context object. This is where you can store the global state that you want to share across components.
const authContext = createContext();

export default authContext;
