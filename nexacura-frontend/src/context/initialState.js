/* This code snippet is defining an initial state object for a JavaScript application. The
`initialState` object contains three properties: */
const initialState = {
  isAuthenticated: false,
  user: {},
  avatarAnswer: "",
};

export default initialState;
